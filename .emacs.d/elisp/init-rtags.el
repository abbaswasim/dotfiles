;;; package --- summary
;;; Commentary:
;;; init-rtags.el rtags-mode settings
; start rtags-mode with Emacs
;;; Code:

(require 'rtags)

;; rtags setup
(setq rtags-display-result-backend 'helm)

(provide 'init-rtags)

;;; init-rtags.el ends here
